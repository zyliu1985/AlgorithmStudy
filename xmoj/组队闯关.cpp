#include <iostream>
#include <algorithm>
using namespace std;

int k[25], p[25], a[25], s[25][25];
int main(){
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	int n, b;
	cin >> n >> b;
	for (int i = 1; i <= n; i++)
		cin >> k[i] >> p[i] >> a[i];
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= n; j++)
			cin >> s[i][j];
	for (int i = )
	return 0;
}

