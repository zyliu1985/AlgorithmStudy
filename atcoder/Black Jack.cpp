#include <iostream>
#include <algorithm>
using namespace std;

int n, l, d;
int dp[200005], stop[200005];
double win(int x){
	if (x > n) return 0;
	if (dp[x]) return dp[x];
	double drop = ;
	double go = 0;
	for (in)
}
int main(){
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	cin >> n >> l >> d;
	build();
	return 0;
}

