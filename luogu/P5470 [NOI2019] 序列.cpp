#include <iostream>
#include <algorithm>
using namespace std;

struct node{
	int a, b;
} x[200005];
void divide(int tp1, int tp2){
	
}
int main(){
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	int t;
	cin >> t;
	while (t--){
		int n, k, l;
		cin >> n >> k >> l;
		for (int i = 1; i <= n; i++)
			cin >> x[i].a;
		for (int i = 1; i <= n; i++)
			cin >> x[i].b;
		
	}
	return 0;
}

