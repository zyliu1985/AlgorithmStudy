#include <iostream>
using namespace std;

int x[200005];
int main(){
	int n, k, cnt = 0;
	cin >> n >> k;
	for (int i = k; i <= n+n; i += k)
		x[++cnt] = i;
	for (int i = 1; i <= cnt; i++)
		
	// A cnt 3
	return 0;
}